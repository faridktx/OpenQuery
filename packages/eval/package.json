{
  "name": "@openquery/eval",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "eval": "pnpm --filter @openquery/core run build && pnpm run build && node dist/index.js",
    "bench": "pnpm --filter @openquery/core run build && pnpm run build && node dist/bench.js",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@openquery/core": "workspace:*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.5.0"
  }
}
