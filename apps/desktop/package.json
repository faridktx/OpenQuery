{
  "name": "@openquery/desktop",
  "version": "0.0.1",
  "description": "OpenQuery Desktop — Tauri app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:bridge": "tsc -p bridge/tsconfig.json",
    "build:bundle": "pnpm run build && pnpm run build:bridge && pnpm tauri build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc -p bridge/tsconfig.json --noEmit",
    "tauri": "tauri",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist bridge/dist"
  },
  "dependencies": {
    "@openquery/core": "workspace:*",
    "@tauri-apps/api": "^2.0.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
